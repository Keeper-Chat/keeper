services:
  thekeeper-server:
    image: ghcr.io/keeper-chat/keeper-server:latest
    pull_policy: always
    environment:
      THEKEEPER_SERVER_ADDR: 0.0.0.0:8787
    ports:
      - "8787:8787"
    restart: unless-stopped
